<!DOCTYPE html>
<html>

<head>
  <title>Fee Receipt</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <style>
    .receipt-container {
      max-width: 600px;
      margin: 50px auto;
      border: 1px solid #ccc;
      padding: 30px;
      border-radius: 10px;
      background-color: #fff;
    }

    .receipt-header {
      text-align: center;
      margin-bottom: 30px;
    }

    .receipt-header h2 {
      margin: 0;
    }

    .receipt-details {
      font-size: 16px;
    }

    .receipt-details .row {
      margin-bottom: 15px;
    }

  @media print {
  .no-print,
  .no-print *,
  .navbar {
    display: none !important;
    visibility: hidden !important;
  }
}
  </style>
</head>

<body class="bg-light">
  <% if(user){ %>
    <%- include('navbar.ejs', { className: "no-print" }) %>
  <div class="receipt-container shadow">
    <div class="receipt-header">
      <h2><%= user.sName %></h2>
      <h6> <%= user.sAddress %></h6>
      <p><strong>Fee Receipt</strong></p>
    </div>

    <% if (result) { %>

      <div class="receipt-details">
        <div class="row">
          <div class="col-6"><strong>Student Name:</strong></div>
          <div class="col-6">
            <%= result.studentName %>
          </div>
        </div>

        <div class="row">
          <div class="col-6"><strong>Receipt No:</strong></div>
          <div class="col-6">
            <%= result.receptNo %>
          </div>
        </div>

        <div class="row">
          <div class="col-6"><strong>Registration No:</strong></div>
          <div class="col-6">
            <%= result.registrationNo %>
          </div>
        </div>

        <div class="row">
          <div class="col-6"><strong>Class</strong></div>
          <div class="col-6">
            <%= result.studentClass %>
          </div>
        </div>

        <div class="row">
          <div class="col-6"><strong>Amount Paid:</strong></div>
          <div class="col-6">₹<%= result.amount %>
          </div>
        </div>

        <div class="row">
          <div class="col-6"><strong>Date:</strong></div>
          <div class="col-6">
           <%= new Date(result.date).toLocaleString("en-IN", { 
    day: "2-digit", 
    month: "long", 
    year: "numeric",
}) %>
          </div>
        </div>

        <div class="row">
          <div class="col-6"><strong>Payment Method:</strong></div>
          <div class="col-6">
            <%= result.paymentMethod %>
          </div>
        </div>
      </div>
        <div class="row">
          <div class="col-6"><strong>Amount Dividation:</strong></div>
          <div class="col-6">
          <h6> Tutiton Fee: ₹<%=(60/100)*(result.amount)  %></h6>
           <h6>Library Fee: ₹<%=(5/100)*(result.amount)  %></h6>
          <h6> Sports Fee: ₹<%=(5/100)*(result.amount)  %></h6>
          <h6> Miscellaneous Fee: ₹<%=(5/100)*(result.amount)  %></h6>
          </div>
        </div>
      </div>

      <div class="text-center mt-4">
        <button class="btn btn-primary no-print" onclick="window.print()">Print Receipt</button>
      </div>
      <div class="text-center mt-4">
        <button class="btn no-print"><a class="btn btn-primary no-print" href="/students/feeReceipt/edit/<%= result.receptNo %>">Edit</a></button>
      </div>
      <div class="text-center mt-4">
        <button class="btn no-print"><a class="btn btn-primary" href="/">Back to Dashboard</a></button>
      </div>

      <% } else { %>
        <div class="alert alert-info text-center">
          No Receipt available for this receipt no with respect to registration no.
        </div>
        <% } %>
  </div>

   <% } else { %>
      <!-- <div class="container d-flex justify-content-center align-items-center" style="height: 80vh">
        <div class="text-center">
          <h2 class="text-danger">
            🚫 You are not authorized to view this page.
          </h2>
          <a href="/login" class="btn btn-primary mt-3">Go to Login</a>
        </div>
      </div> -->
       <div class="d-flex justify-content-center align-items-center vh-100 bg-light text-center">
    <div class="card p-5 shadow rounded">
      <h1 class="text-danger mb-4">403 - Access Denied</h1>
      <p class="lead">🚫 You are <strong>not authorized</strong> to access this functionality.</p>
      <a href="/" class="btn btn-primary mt-3">Go to Home</a>
    </div>
  </div>
      <% } %>
</body>

</html>