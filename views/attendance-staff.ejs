<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Staff Attendance Marking</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #e3f2fd, #f8f9fa);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .container {
      background: #fff;
      padding: 35px;
      border-radius: 16px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
      max-width: 1000px;
      width: 100%;
    }
    h2 {
      font-weight: 700;
      color: #0d6efd;
      margin-bottom: 10px;
      text-align: center;
    }
    h4 {
      font-size: 1rem;
      color: #555;
      text-align: center;
      margin-bottom: 20px;
    }
    table {
      border-radius: 12px;
      overflow: hidden;
    }
    thead th {
      background: #0d6efd;
      color: white;
      text-align: center;
      vertical-align: middle;
      font-weight: 600;
    }
    tbody td {
      text-align: center;
      vertical-align: middle;
    }
    tbody tr:hover {
      background-color: #f1f8ff;
    }
    .attendance-radio {
      transform: scale(1.3);
      cursor: pointer;
    }
    .btn-primary {
      display: block;
      margin: 25px auto 0 auto;
      padding: 12px 35px;
      font-size: 18px;
      border-radius: 10px;
      font-weight: 600;
      background: linear-gradient(135deg, #0d6efd, #0056b3);
      border: none;
      transition: 0.3s ease-in-out;
    }
    .btn-primary:hover {
      background: linear-gradient(135deg, #0056b3, #0d6efd);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }
    .header-divider {
      height: 2px;
      width: 60px;
      background: #0d6efd;
      margin: 10px auto 25px auto;
      border-radius: 5px;
    }
  </style>
</head>
<body>

  <div class="container">
    <h2>📋 Staff Attendance Marking</h2>
    <div class="header-divider"></div>
    <h4>
      Job Title: <b><%= jobTitle %></b> | 
      Date: <b><%= date %></b>
    </h4>

    <form method="POST" action="/staffAttendance/staff/mark">
      <input type="hidden" name="date" value="<%= date %>">
      <input type="hidden" name="jobTitle" value="<%= jobTitle %>">

      <div class="table-responsive">
        <table class="table table-bordered table-hover align-middle">
          <thead>
            <tr>
              <th>S.No.</th>
              <th>Staff Name</th>
                <th>Attendance</th>
            </tr>
          </thead>
          <tbody>
            <% staff.forEach((st, index )=> { %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= st.teacherName %></td>
               <td>
          <input type="hidden" name="records[<%= index %>][staffId]" value="<%= st._id %>">
         <div class="d-flex justify-content-center gap-3">
    <div class="form-check">
      <input class="form-check-input attendance-radio" type="radio" 
             name="records[<%= index %>][status]" 
             value="Present" id="present-<%= index %>" >
      <label class="form-check-label" for="present-<%= index %>">Present</label>
    </div>
    <div class="form-check">
      <input class="form-check-input attendance-radio" type="radio" 
             name="records[<%= index %>][status]" 
             value="Absent" id="absent-<%= index %>">
      <label class="form-check-label" for="absent-<%= index %>">Absent</label>
    </div>
    <div class="form-check">
      <input class="form-check-input attendance-radio" type="radio" 
             name="records[<%= index %>][status]" 
             value="Leave" id="leave-<%= index %>" checked>
      <label class="form-check-label" for="leave-<%= index %>">Leave</label>
    </div>
  </div>
        </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>

      <button type="submit" class="btn btn-primary">💾 Save Attendance</button>
    </form>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
