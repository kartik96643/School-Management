<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Delete Multiple Students</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
   /* ===== Base Reset & Layout ===== */
body {
  margin: 0;
  padding: 0;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
}

.container {
  width: 95%;
  max-width: 960px;
  padding: 20px;
}

/* ===== Card with Glassmorphism Effect ===== */
.custom-card {
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(12px);
  border-radius: 20px;
  border: 1px solid rgba(255, 255, 255, 0.3);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
  overflow: hidden;
  transition: transform 0.3s ease;
}

.custom-card:hover {
  transform: translateY(-4px);
}

/* ===== Header Styling ===== */
.card-header {
  background: linear-gradient(135deg, #4f46e5, #9333ea);
  color: #fff !important;
  padding: 20px;
  border-top-left-radius: 20px;
  border-top-right-radius: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.card-header h5 {
  font-size: 1.4rem;
  font-weight: 600;
  margin: 0;
}

.card-header .btn {
  background: #ffffff;
  color: #4f46e5;
  border: none;
  border-radius: 8px;
  padding: 8px 14px;
  font-weight: bold;
  transition: all 0.3s ease;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

.card-header .btn:hover {
  background: #e0e7ff;
  transform: scale(1.05);
}

/* ===== Table Section ===== */
.card-body {
  padding: 25px;
}

.table-responsive {
  border-radius: 12px;
  overflow: hidden;
}

.table {
  width: 100%;
  border-collapse: collapse;
  background: #fff;
  border-radius: 10px;
  overflow: hidden;
  animation: fadeIn 0.8s ease-in-out;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  } to {
    opacity: 1;
    transform: translateY(0);
  }
}

.table thead {
  background: linear-gradient(135deg, #6366f1, #a855f7);
  color: white;
}

.table th,
.table td {
  padding: 14px;
  border: none;
  text-align: center;
}

.table-striped tbody tr:nth-of-type(odd) {
  background-color: #f3f4f6;
}

/* ===== Checkbox Styling ===== */
input[type="checkbox"] {
  width: 20px;
  height: 20px;
  accent-color: #8b5cf6;
  cursor: pointer;
  transition: 0.2s ease;
}

input[type="checkbox"]:hover {
  box-shadow: 0 0 5px #8b5cf6;
  transform: scale(1.1);
}

/* ===== Delete Button Styling ===== */
.btn-danger {
  background: linear-gradient(to right, #ef4444, #dc2626);
  color: #fff;
  border: none;
  padding: 12px 24px;
  font-weight: 600;
  font-size: 1rem;
  border-radius: 10px;
  margin-top: 20px;
  transition: all 0.3s ease;
  box-shadow: 0 8px 20px rgba(239, 68, 68, 0.3);
}

.btn-danger:hover {
  background: linear-gradient(to right, #dc2626, #b91c1c);
  transform: scale(1.05);
}

/* ===== Message if No Students Found ===== */
h1 {
  text-align: center;
  color: #6b7280;
  margin-top: 30px;
  font-size: 1.5rem;
}

  </style>
</head>
<body>
<div class="container">

  <div class="card custom-card">
    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
      <h5 class="mb-0">Delete Multiple Students</h5>
      <a href="/students/classes/<%= students[0].studentClass %>?medium=<%= students[0].medium %><% if (students[0].studentClass === '11' || students[0].studentClass === '12') { %>&stream=<%= students[0].stream %><% } %>" class="btn btn-light btn-sm">
        ← Back to List
      </a>
    </div>

    <% if(students && students.length > 0){ %>

    <div class="card-body">
      <form action="/students/delete-multiple/<%= students[0].studentClass  %>/<%= students[0].medium %><% if (students[0].studentClass === '11' || students[0].studentClass === '12') { %>/<%= students[0].stream %><% } %>" method="POST" onsubmit="return confirm('Are you sure you want to delete the selected students?');">
        <div class="table-responsive">
          <table class="table table-striped table-bordered align-middle text-center">
            <thead>
              <tr>
                <th><input type="checkbox" id="selectAll" title="Select All"> Select All</th>
                <th>Student Name</th>
                <th>Registration No</th>
              </tr>
            </thead>
            <tbody>
              <% students.forEach((student, index) => { %>
                <tr>
                  <td><input type="checkbox" name="registrationNos[]" value="<%= student.registrationNo %>"></td>
                  <td><%= student.studentName %></td>
                  <td><%= student.registrationNo %></td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
        <button type="submit" class="btn btn-danger mt-3">
          🗑️ Delete Selected Students
        </button>
      </form>
    </div>
    <% }else{ %>
<h1>No students found for this class</h1>
        <% } %>
  </div>
</div>

<script>
  document.getElementById('selectAll').addEventListener('click', function () {
    const checkboxes = document.querySelectorAll('input[name="registrationNos[]"]');
    checkboxes.forEach(cb => cb.checked = this.checked);
  });
</script>

</body>
</html>
