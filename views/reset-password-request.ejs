<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Password</title>
    <style>
   body {
    min-height: 100vh;
    margin: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-image: url("/assets/signup.jpg");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    position: relative;
    overflow: hidden;
}

body::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.4);
    z-index: 0;
}

.signin-card {
    position: relative;
    z-index: 1;
    background: rgba(255, 255, 255, 0.15);
    border-radius: 20px;
    padding: 2.5rem;
    max-width: 520px; /* Increased width */
    width: 90%;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.18);
    box-shadow: 0 8px 32px rgba(31, 38, 135, 0.37);
    color: #fff;
    text-align: center;
}

.signin-card h3 {
    font-weight: 600;
    color: #ffffff;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
}

.form-label {
    font-weight: 500;
    color: #ccc;
    font-size: 0.95rem;
    margin-bottom: 1rem;
}

.form-heading {
    display: block;
    font-size: 1.1rem;
    font-weight: 600;
    color: #f0f0f0;
}

.form-body {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.form-control {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: #fff;
    padding: 12px 16px;
    border-radius: 10px;
    width: 100%;
    max-width: 400px;
    text-align: left;
}

.form-control::placeholder {
    color: rgba(255, 255, 255, 0.7);
}

.form-control:focus {
    background-color: rgba(255, 255, 255, 0.15);
    border-color: #f093fb;
    box-shadow: 0 0 0 0.2rem rgba(240, 147, 251, 0.25);
    color: #fff;
    outline: none;
}

.btn-primary {
    background: linear-gradient(to right, #f093fb, #f5576c);
    border: none;
    font-weight: bold;
    padding: 12px;
    margin-top: 10px;
    width: 100%;
    max-width: 320px; /* ⬅️ Changed from 400px to 320px */
    border-radius: 10px;
    transition: all 0.3s ease-in-out;
    color: white;
}


.btn-primary:hover {
    background: linear-gradient(to right, #f5576c, #f093fb);
    transform: scale(1.02);
}

.form-text {
    font-size: 0.9rem;
    color: #e0e0e0;
}

.alert {
    background-color: rgba(255, 255, 255, 0.2);
    border: none;
    backdrop-filter: blur(5px);
    color: #fff;
    padding: 10px 15px;
    border-radius: 8px;
    margin-bottom: 1rem;
}

a.text-primary {
    color: #f093fb !important;
    text-decoration: none;
}

a.text-primary:hover {
    color: #f5576c !important;
    text-decoration: underline;
}


    </style>
  </head>
  <body>
  <form method="POST" action="/admin/reset-password" class="signin-card">
  <h3 class="text-center mb-3">🔒 Forgot Password?</h3>
  <p class="form-label text-center mb-4">Please enter your registered email to receive the reset link.</p>

  <% if(error){ %>
    <div class="alert alert-danger text-center"><%= error %></div>
  <% } %>

  <% if(message){ %>
    <div class="alert alert-success text-center"><%= message %></div>
  <% } %>

  <div class="form-body text-center">
    <label for="email" class="form-heading mb-2">Email Address</label>
    <input name="email" type="email" id="email" class="form-control mb-3 mx-auto" placeholder="Enter your email" required />

    <button type="submit" class="btn btn-primary">Send Reset Link</button>
  </div>

  <p class="form-text text-center mt-4">
    Remembered your password? <a href="/admin/signin" class="text-primary">Login</a>
  </p>
</form>



  </body>
</html>
