<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        <%= jobTitle.charAt(0).toUpperCase() + jobTitle.slice(1) %> Staff Details
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body.bg-light {
            background: linear-gradient(to bottom right, #e3f2fd, #e8f5e9);
            font-family: "Segoe UI", Roboto, sans-serif;
        }

        h2.text-primary {
            font-weight: 700;
            font-size: 2.4rem;
            margin-bottom: 1rem;
            text-align: center;
            color: #0d6efd;
        }

        .table-container {
            background-color: #fff;
            border-radius: 16px;
            padding: 25px;
            box-shadow: 0 12px 28px rgba(0, 0, 0, 0.06);
            margin-top: 20px;
        }

        .table th {
            background-color: #0d6efd;
            color: #fff;
        }

        .btn-outline-primary {
            border-radius: 50px;
            font-weight: 600;
        }

        .alert-warning {
            background-color: #fff3cd;
            color: #856404;
            border-radius: 10px;
            border: 1px solid #ffeeba;
            padding: 15px 20px;
            font-size: 1rem;
        }
    </style>
</head>

<body class="bg-light">

    <% if (locals.user) { %>
        <%- include('navbar.ejs') %>

            <div class="container py-5">
                <h2 class="text-primary">
                    <%= jobTitle.charAt(0).toUpperCase() + jobTitle.slice(1) %> Details
                </h2>

                <% if (staff && staff.length> 0) { %>
                    <div class="table-responsive table-container">
                        <table class="table table-bordered table-striped table-hover text-center">
                            <thead>
                                <tr>
                                    <th>S.No.</th>
                                    <th>Employee Id</th>
                                    <th>Name</th>
                                    <th>Phone</th>
                                    <th>Email</th>
                                    <th>Salary</th>
                                    <th>Gender</th>
                                    <th colspan="2">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% staff.forEach((person, index)=> { %>
                                    <tr>
                                        <td>
                                            <%= index + 1 %>
                                        </td>
                                        <td>
                                            <%= person.empId || "N/A" %>
                                        </td>
                                        <td>
                                            <%= person.teacherName || "N/A" %>
                                        </td>
                                        <td>
                                            <%= person.teacherContact || "N/A" %>
                                        </td>
                                        <td>
                                            <%= person.teacherEmail || "N/A" %>
                                        </td>


                                        <td>
                                            <%= person.teacherSalary %>
                                        </td>


                                        <td>
                                            <%= person.gender || "N/A" %>
                                        </td>
                                        <td id="edit-btn"><a href="/teacher/edit/<%= person.teacherEmail %>" class="btn btn-outline-primary btn-sm">
                                                Edit
                                            </a></td>

                                        <td id="edit-btn"><a
                                                href="/teacher/delete/<%= person.teacherEmail %>"
                                                class="btn btn-outline-primary btn-sm">
                                                Delete
                                            </a></td>
                                    </tr>
                                    <% }) %>
                            </tbody>
                        </table>
                    </div>
                    <% } else { %>
                        <div class="alert alert-warning text-center">
                            No <%= jobTitle %> staff found.
                        </div>
                        <% } %>

                            <div class="text-center mt-4">
                                <a href="/teacher/list" class="btn btn-outline-primary">⬅ Back to Staff Summary</a>
                            </div>
            </div>

            <% } else { %>
                <div class="container d-flex justify-content-center align-items-center" style="height: 80vh;">
                    <div class="text-center">
                        <h2 class="text-danger">🚫 You are not authorized to view this page.</h2>
                        <a href="/login" class="btn btn-primary mt-3">Go to Login</a>
                    </div>
                </div>
                <% } %>

</body>

</html>