<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Check Passout Students</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f0f8ff;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .form-container {
      max-width: 600px;
      margin: 60px auto;
      background: #fff;
      padding: 40px;
      border-radius: 16px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
    }

    h2 {
      color: #0d6efd;
      margin-bottom: 30px;
    }

    .btn-primary {
      border-radius: 50px;
      padding: 10px 30px;
      font-weight: 600;
    }

    .form-label {
      font-weight: 600;
    }
  </style>
</head>
<body>
  <%- include('navbar.ejs') %>

  <div class="form-container">
    <h2 class="text-center">📘 Enter Session for Passout Students</h2>
    <form method="POST" action="/students/passout">
      <div class="mb-3">
        <label for="session" class="form-label">Academic Session</label>
        <input
          type="text"
          class="form-control"
          id="session"
          name="session"
          placeholder="e.g. 2024-25"
          required
        >
      </div>

        <div class="mb-3">
                  <label for="stream" class="form-label">Select Stream</label>
                  <select class="form-select" id="stream" name="stream" required>
                    <option value="">-- Select Stream --</option>
                    <option value="Arts" >Arts</option>
                    <option value="Commerce" >Commerce</option>
                    <option value="Science" >Science</option>
                  </select>
                </div>

      <!-- Hidden class field prefilled as Passout -->
      <input type="hidden" name="studentClass" value="Passout">

      <div class="text-center">
        <button type="submit" class="btn btn-primary">✅ View Passout Students</button>
      </div>
      <div class="text-center mt-3">
          <a href="/" class="btn btn-primary">⬅ Back to Home</a>
      </div>
    </form>
  </div>

</body>
</html>
