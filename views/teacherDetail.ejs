<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Class Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
  <style>
    body.bg-light {
      background: linear-gradient(to bottom right, #e3f2fd, #e8f5e9);
      font-family: "Segoe UI", Roboto, sans-serif;
      color: #333;
      margin: 0;
      padding: 0;
    }

    h2.text-primary {
      font-weight: 700;
      font-size: 2.4rem;
      margin-bottom: 1rem;
      text-align: center;
      position: relative;
      color: #0d6efd;
    }

    h2.text-primary::after {
      content: '';
      width: 80px;
      height: 4px;
      background: #0d6efd;
      display: block;
      margin: 12px auto 0;
      border-radius: 5px;
    }

    form .form-select {
      border-radius: 30px;
      padding: 10px 20px;
      border: 1px solid #ced4da;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
      transition: 0.3s ease;
    }

    form .form-select:focus {
      border-color: #0d6efd;
      box-shadow: 0 0 6px rgba(13, 110, 253, 0.4);
    }

    .table-container {
      background-color: #fff;
      border-radius: 16px;
      padding: 25px;
      box-shadow: 0 12px 28px rgba(0, 0, 0, 0.06);
      margin-top: 20px;
    }

    .table {
      margin-bottom: 0;
      border-radius: 10px;
      overflow: hidden;
    }

    .table th,
    .table td {
      text-align: center;
      vertical-align: middle;
      padding: 14px;
    }

    .table th {
      background-color: #0d6efd;
      color: #fff;
      font-size: 1rem;
      font-weight: 600;
    }

    .table-striped tbody tr:nth-child(even) {
      background-color: #f4f8fb;
    }

    .table-hover tbody tr:hover {
      background-color: #e0f0ff;
    }

    td a.btn {
      border-radius: 50px;
      font-size: 0.85rem;
      padding: 6px 14px;
      font-weight: 500;
      transition: all 0.3s ease-in-out;
    }

    td a.btn-outline-primary:hover {
      background-color: #0d6efd;
      color: #fff;
    }

    td a.btn-outline-danger:hover {
      background-color: #dc3545;
      color: #fff;
      border-color: #dc3545;
    }

    td a.btn-outline-success:hover {
      background-color: #198754;
      color: #fff;
      border-color: #198754;
    }

    .table tbody .fw-bold {
      background-color: #e3f2fd;
      font-size: 1.05rem;
      font-weight: 600;
    }

    .custom-btn-outline,
    .custom-btn-success {
      font-weight: 600;
      border-radius: 30px;
      padding: 10px 20px;
      transition: 0.3s;
      display: inline-block;
    }

    .custom-btn-outline {
      border: 2px solid #0d6efd;
      color: #0d6efd;
    }

    .custom-btn-outline:hover {
      background-color: #0d6efd;
      color: #fff;
    }

    .custom-btn-success {
      background-color: #28a745;
      color: white;
      border: none;
    }

    .custom-btn-success:hover {
      background-color: #218838;
    }

    .text-danger {
      font-size: 1.8rem;
      font-weight: bold;
      text-align: center;
    }

    .alert-warning {
      background-color: #fff3cd;
      color: #856404;
      border-radius: 10px;
      border: 1px solid #ffeeba;
      padding: 15px 20px;
      font-size: 1rem;
    }

    @media print {
      .no-print {
        display: none !important;
      }

      body.bg-light {
        background: white;
      }

      .table-container {
        box-shadow: none;
        padding: 0;
        margin: 0;
      }
    }
  </style>
</head>

<body class="bg-light">

  <% if (locals.user) { %>
  <div class="container py-5">
    <h2 class="text-center mb-4 text-primary">Teacher Details</h2>
    <form action="" method="get" class="mb-4">
      <div class="row g-2 justify-content-center">
        <div class="col-md-3">
          <select name="sort" class="form-select" onchange="this.form.submit()">
            <option disabled <%=!query?.sort ? 'selected' : '' %>>Sort By</option>
            <option value="nameAsc" <%=query?.sort==='nameAsc' ? 'selected' : '' %>>Name (A → Z)</option>
            <option value="nameDesc" <%=query?.sort==='nameDesc' ? 'selected' : '' %>>Name (Z → A)</option>
          </select>
        </div>
      </div>
    </form>

    <% if (teachers && teachers.length> 0) { %>

    <% 
      let teacherSalary=0;
      teachers.forEach(teacher=>{
        teacherSalary +=teacher.teacherSalary
      })
    %>

    <div class="table-responsive table-container">
      <table class="table table-bordered table-striped table-hover">
        <thead class="text-center">
          <tr>
            <th scope="col">S.No.</th>
            <th scope="col">Name</th>
            <th scope="col">Contact No.</th>
            <th scope="col">Employee Id</th>
            <th scope="col">Classes</th>
            <th scope="col">Salary</th>
            <th scope="col">Email</th>
            <th scope="col">Gender</th>
            <th scope="col">Medium</th>
            <th scope="col" colspan="3">Actions</th>
          </tr>
        </thead>
        <tbody>
          <% teachers.forEach((teacher, index)=> { %>
          <tr>
            <td class="text-center"><%= index + 1 %></td>
            <td class="text-center"><%= teacher.teacherName %></td>
            <td class="text-center"><%= teacher.teacherContact %></td>
            <td class="text-center"><%= teacher.empId %></td>
            <td class="text-center"><%= teacher.teacherClass %></td>
            <td class="text-center"><%= teacher.teacherSalary %></td>
            <td class="text-center"><%= teacher.teacherEmail %></td>
            <td class="text-center"><%= teacher.gender %></td>
            <td class="text-center"><%= teacher.medium %></td>
            <td class="text-center"><a href="/teacher/edit/<%= teacher.teacherEmail %>" class="btn btn-outline-primary btn-sm">Edit</a></td>
            <td class="text-center"><a href="/teacher/delete/<%= teacher.teacherEmail %>" class="btn btn-outline-primary btn-sm" onclick="return confirm('Are you sure you want to delete this teacher?')">Delete</a></td>
            <td class="text-center"><a href="/teacher/details/email/<%= teacher.teacherEmail %>" class="btn btn-outline-primary btn-sm">Details</a></td>
          </tr>
          <% }) %>
          <tr class="text-center fw-bold">
            <td colspan="4">Total Teachers: <%= teachers.length %></td>
            <td> Total: <%= teacherSalary %></td>
            <td colspan="6"></td>
          </tr>
        </tbody>
      </table>
    </div>
    <% } else { %>
    <div class="alert alert-warning text-center">
      No teacher found for this Subject.
    </div>
    <% } %>

    <div class="text-center mt-4">
      <a href="/teacher/list" class="btn btn-outline-primary">⬅ Back to All Subjects</a>
    </div>
  </div>
  <% } else { %>
  <div class="container d-flex justify-content-center align-items-center" style="height: 80vh;">
    <div class="text-center">
      <h2 class="text-danger">🚫 You are not authorized to view this page.</h2>
      <a href="/login" class="btn btn-primary mt-3">Go to Login</a>
    </div>
  </div>
  <% } %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO"
    crossorigin="anonymous"></script>
</body>

</html>
