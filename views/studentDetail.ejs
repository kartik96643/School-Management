<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Student Details</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #eef2f7;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      .card {
        max-width: 800px;
        margin: 60px auto;
        padding: 35px 40px;
        border-radius: 15px;
        background-color: #ffffff;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      }

      h3 {
        font-weight: 600;
        color: #007bff;
      }

      .label {
        font-weight: 600;
        color: #343a40;
      }

      .value {
        color: #495057;
      }

      .row.mb-3 {
        padding: 6px 0;
        border-bottom: 1px solid #dee2e6;
      }

      .btn {
        min-width: 140px;
      }

      @media print {
        .no-print {
          display: none !important;
        }

        body {
          background-color: white;
        }

        .card {
          box-shadow: none;
          border: none;
          margin: 0;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="card">
        <h3 class="text-center mb-4">📘 Student Full Details</h3>

        <div class="row mb-3">
          <div class="col-md-6">
            <span class="label">Student Name:</span>
            <span class="value"><%= student.studentName %></span>
          </div>
          <div class="col-md-6">
            <span class="label">Father's Name:</span>
            <span class="value"><%= student.fatherName %></span>
          </div>
        </div>

        <div class="row mb-3">
          <div class="col-md-6">
            <span class="label">Mother's Name:</span>
            <span class="value"><%= student.motherName %></span>
          </div>
          <div class="col-md-6">
            <span class="label">Dob:</span>
            <span class="value"><%= student.studentDob.toISOString().split('T')[0] %></span>
          </div>
        </div>
        

        <div class="row mb-3">
          <div class="col-md-6">
            <span class="label">Gender:</span>
            <span class="value"><%= student.gender %></span>
          </div>
          <div class="col-md-6">
            <span class="label">Session:</span>
            <span class="value"><%= student.session %></span>
          </div>
        </div>

        <div class="row mb-3">
          <div class="col-md-6">
            <span class="label">Class:</span>
            <span class="value"><%= student.studentClass %></span>
          </div>
          <% if(student.studentClass == '11' || student.studentClass == '12') { %>
          <div class="col-md-6">
            <span class="label">Stream:</span>
            <span class="value"><%= student.stream %></span>
          </div>
          <% } else { %>
          <div class="col-md-6">
            <span class="label">Stream:</span>
            <span class="value">N/A</span>
          </div>
          <% } %>
        </div>

        <div class="row mb-3">
          <div class="col-md-6">
            <span class="label">Medium:</span>
            <span class="value"><%= student.medium %></span>
          </div>
          <div class="col-md-6">
            <span class="label">Contact Number:</span>
            <span class="value"><%= student.contactNo %></span>
          </div>
        </div>

        <div class="row mb-3">
          <div class="col-md-6">
            <span class="label">Registration No.:</span>
            <span class="value"><%= student.registrationNo %></span>
          </div>
          <div class="col-md-6">
            <span class="label">Total Fees:</span>
            <span class="value">₹<%= student.totalFees %></span>
          </div>
        </div>

        <div class="row mb-3">
          <div class="col-md-6">
            <span class="label">Fees Paid:</span>
            <span class="value">₹<%= student.feesPaid %></span>
          </div>
          <div class="col-md-6">
            <span class="label">Fees Remaining:</span>
            <span class="value">₹<%= student.totalFees - student.feesPaid %></span>
          </div>
        </div>
        <div class="col-md-6">
          <span class="label">Address</span>
          <span class="value"><%= student.address %></span>
        </div>

        <div class="text-center mt-4 no-print">
          <a
            href="/students/classes/<%= student.studentClass %>?medium=<%= student.medium %><% if (student.studentClass === '11' || student.studentClass === '12') { %>&stream=<%= student.stream %><% } %>"
            class="btn btn-outline-primary me-2"
            >🔙 Back</a
          >
          <a
            href="/students/edit/<%= student.registrationNo %>"
            class="btn btn-outline-warning me-2"
            >✏️ Edit</a
          >
          <a
            href="/students/delete/<%= student.registrationNo %>"
            class="btn btn-outline-danger me-2"
            onclick="return confirm('Are you sure you want to delete this student?')"
            >🗑️ Delete</a
          >
          <button
            type="button"
            class="btn btn-outline-dark"
            onclick="window.print()"
          >
            🖨️ Print
          </button>
        </div>
      </div>
    </div>
  </body>
</html>